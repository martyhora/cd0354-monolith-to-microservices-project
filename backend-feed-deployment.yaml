apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-feed-app
  labels:
    app: backend-feed-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend-feed-app
  template:
    metadata:
      labels:
        app: backend-feed-app
    spec:
      containers:
        - name: backend-feed
          image: mhora1/udagram-api-feed:v1
          ports:
            - containerPort: 80
      imagePullSecrets:
        - name: docker-secrets
